const e=`<template>
  <an-textarea placeholder="你站的方位，跟我中间隔着泪。" v-model="textareaValue" readonly/>
</template>

<script setup>
import {ref} from 'vue'

const textareaValue = ref('')
<\/script>
`;export{e as default};
